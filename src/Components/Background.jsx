import React from 'react';

const Background = () => {
  return (
    <div className="fixed inset-0 z-0 bg-gray-700">
      {/* Optional: Add content or design to your background if needed */}
    </div>
  );
};

export default Background;